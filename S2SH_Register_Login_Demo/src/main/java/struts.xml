<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
    <constant name="struts.custom.i18n.resources" value="i18n/MessageResources"></constant>
    <package name="demo-root" namespace="/" extends="struts-default">
        <interceptors>
            <interceptor-stack name="tokenStack">
                <interceptor-ref name="token">
                    <param name="excludeMethods">input</param>
                </interceptor-ref>
                <interceptor-ref name="defaultStack"/>
            </interceptor-stack>
        </interceptors>

        <!-- results made available to all actions -->
        <global-results>
            <result name="success">/pages/welcome.jsp</result>
        </global-results>
        <action name="login" method="{1}" class="chinasei.struts2.user.web.UserAction" >
            <result name="input">/login.jsp</result>
        </action>

        <action name="user" method="{1}" class="chinasei.struts2.user.web.UserAction">
            <result name="userlist">/pages/users.jsp</result>
        </action>

        <action name="regist" method="{1}" class="chinasei.struts2.user.web.UserAction">
            <interceptor-ref name="tokenStack"></interceptor-ref>
            <result name="invalid.token">/pages/regist.jsp</result>
            <result name="input">/pages/regist.jsp</result>
        </action>
    </package>
</struts>